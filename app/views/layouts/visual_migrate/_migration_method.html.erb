<table border="0" style="margin-left:2em;">
  <tr>
    <th width="80em">Method</th>
    <td><%= method_select '[methods][' + @migration_method.name + '][name]', @migration_method.name %></td>
    <td><%= check_box_tag '[methods][' + @migration_method.name + '[enable]', true, @enable %>enable　</td>
  </tr>
</table>
<% @migration_method.funcs.each do |key, val| %>
  <%= show_func(val) %> 
<% end %>
